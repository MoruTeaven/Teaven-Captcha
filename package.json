{
  "name": "teaven-captcha",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply teaven-captcha --local",
    "db:migrate:remote": "wrangler d1 migrations apply teaven-captcha --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.60.0"
  }
}
